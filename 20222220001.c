#include <stdio.h>
int main(){
	float num;
	float soma=0;
	
	scanf("%f", &num);
	soma = soma + num;
	scanf("%f", &num);
	soma = soma + num;
	scanf("%f", &num);
	soma = soma + num;
	scanf("%f", &num);
	soma = soma + num;
	scanf("%f", &num);
	soma = soma + num;
	printf("\nsoma = %.2f", soma);
	
	return 0;
}
